// logical operator "AND"
console.log(true && true )// all values have to be TRUE for expresin to be TRUE

// logical operator "OR"
console.log(true || false )// one value have to be TRUE for expresin to be TRUE

// logical operator "NOT"
console.log(!true)
//ex
console.log( 6 == 10)
